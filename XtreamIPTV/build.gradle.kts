version = 2

cloudstream {
    // Description of the plugin
    description = "Watch Live TV, Movies & Series from your Xtream Codes IPTV provider directly in CloudStream. Supports Live channels (HLS/MPEG-TS), VOD movies, and full series with episodes."
    // Authors
    authors = listOf("XtreamIPTV")
    // Language
    language = "en"
    // Status: 0 = down, 1 = ok, 2 = slow
    status = 1
    // Supported TV types
    tvTypes = listOf("Live", "Movie", "TvSeries")
    // Icon URL (optional)
    iconUrl = ""
    // Does not require external resources
    requiresResources = false
}
